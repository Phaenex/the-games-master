{
  "name": "the-games-master",
  "version": "0.1.0",
  "private": true,
  "description": "A first-person psychological-horror card game — Three.js, no build step.",
  "scripts": {
    "serve": "npx serve .",
    "test": "node scripts/run-tests.mjs"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
