# Handoff — 2026-08-17

This chat is the working trunk: **Main: audit, commit, plan**.

Repo: `/Users/damato/Projects/games/the-games-master`  
Branch: `wend-prologue-boundary-navmesh-harnesses`  
Latest pushed hub commit: **`a9dc553`** (cellar climb-out). Latest push overall: **`ebfe0f2`** (Parlor 1080p series qualified). House recovery for unreadable envelopes is in the working tree, not pushed. Ordinary New Run stays clean.

Do not use `/Users/damato/Games Master`. That is the April skeleton.

No push. Do not commit unless Nick asks.

Read `docs/TESTING.md` before any Unity job. Unity must be closed. Do not chain Unity jobs. Trust only the log written by the current run.

## Where to start

1. `docs/superpowers/plans/2026-08-17-main-continuation.md`, the live plan
2. `docs/TESTING.md`
3. This file

`docs/HANDOFF-2026-08-16.md` remains the last full opening-gate evidence pack. Do not treat it as the current content inventory.

## What this retrospective actually proved (2026-08-18)

Prior H1–H6 grades were inherited. This run re-verified them. Do not copy the old "campaign walk covers foyer → parlor" claim. That test teleported between rooms, asserted a tautology at spawn, and let the parlor trigger pass with `Contains || x > 5.4`.

**Mechanical this run (XML, not memory):** EditMode **963/963** including House recovery, post-Mirror Ledger Review, crash-resumable Restore Last Valid and redacted support diagnostics. PlayMode is freshly **44/44**. Boot rebuild, audit and seven-state tour are green twice consecutively, with every frame inspected. Parlor 1080p series qualified on `ebfe0f2`.

Frames: `unity-project/Screens/EntryHall/tour-01` through `tour-25` from this tour. Docs copies match that run: `docs/playtest/screenshots/entry-hall-tour-*.png` (25) and `parlor-tour-*.png` (24).

### Session-by-session

| Session | Function | Visual | Notes |
|---|---|---|---|
| H1 foyer / stairs / Percival / locks | CONFIRMED | PARTIAL | Connected spawn walk. Front doors stay barred. Shot 11 chandelier still blows white. |
| H2 library + Weighted Shelf | CONFIRMED | PARTIAL | Aisle walk and puzzle still hold. Books are `SM_Book` meshes now (test asserts the mesh path). Tour-14 at shelf distance still reads as five standing volumes. |
| H3 gallery / Marr / barred guest | CONFIRMED | PARTIAL | Connected walk into Marr. Barred guest holds a body. Shot 17 is a bed rail and a sconce. The bar is not in that frame. |
| H4 attic | CONFIRMED | PARTIAL | Connected climb and descent. Tread rise/run is measured, not a name-prefix count. Hatch still reads as an orange opening. |
| H5 cellar / vault | CONFIRMED | PARTIAL | Connected spawn → well → vault grate → climb back onto HallFloorEast. Shot 22 looks down the well; stairs read in shot 07. |
| H6 Court / STB doors | CONFIRMED | PARTIAL | Locks and scene ids hold. Conservatory stays barred. Shots 24–25 are in the sconce pool. |
| Parlor editor table | CONFIRMED (prior run) | PARTIAL | Isolation retake 24/24. Built-player 1080p series **3/3 + coverage** qualified (`cb62fa53…`). Aldric is still the proxy. Lantern-pool look. |
| House recovery (unreadable envelope) | CONFIRMED | not shot | Isolated New Run does not teach. Reset is two confirms, then quarantine + genesis. EditMode **949/949**. |
| Ledger Review | CONFIRMED | CONFIRMED | Unlocks only after a completed Mirror run. Copy states its completed-run sample and uncertainty. `boot-tour-02-ledger-interpretation.png`. |
| Restore Last Valid | CONFIRMED | CONFIRMED | Offered only when a complete predecessor validates. Continue is durably cleared before profile mutation. `boot-tour-03/04`. |
| Support diagnostics | CONFIRMED | CONFIRMED UI | Redacted preview plus visible cancel/success feedback, including high contrast. `boot-tour-05/06/07`. Real macOS panel remains a release-candidate human check. |

### What changed in this review

- Campaign walk starts at the real Player spawn and `Move`s. No per-room teleport. Parlor finish is `bounds.Contains(capsuleCenter)` with no `||` escape.
- Separate connected walk: spawn → cellar panel → vault grate trigger.
- Consecutive stair / ladder / cellar treads must match authored rise and run.
- Weighted shelf uses WitchVillage `SM_Book` prefabs instead of rounded boxes.
- Cellar tread run is 0.24 m. Top tread sits at the east panel. `HallFloorMidSouth` stops south of the well so its north face is not a wall across a 1.8 m capsule still on the flight. Vault north wall is open across the well x. Connected climb-out onto HallFloorEast is EditMode green.

## Next work (do not skip ahead)

1. Exercise the real macOS diagnostics save panel once in a disposable release-candidate profile.
2. Five missing table games. Do not invent rules for games 6–7.
3. Nick's Phase 0 walk. Then Court.

## Human gates still open

Nick’s Phase 0 walk, display/audio/feel, vehicle taste, cellar damask vs coaching-inn cellar, whether the guest room and loft are dressed enough, Aldric character art, Parlor table readability. Court locked. Do not claim "perfect," "done," or "ready for Nick" from tests alone.
