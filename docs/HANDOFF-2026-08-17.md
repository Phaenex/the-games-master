# Handoff — 2026-08-17

This chat is the working trunk: **Main: audit, commit, plan**.

Repo: `/Users/damato/Projects/games/the-games-master`  
Branch: `wend-prologue-boundary-navmesh-harnesses` (64 local commits ahead of origin, plus this snapshot)  
Do not use `/Users/damato/Games Master`. That is the April skeleton.

No push. Nick authorized this commit so the uncommitted 08-16/08-17 tree stops living only in dirty working copies.

Read `docs/TESTING.md` before any Unity job. Unity must be closed. Do not chain Unity jobs. Trust only the log written by the current run.

## Where to start

1. `docs/superpowers/plans/2026-08-17-main-continuation.md` — the live plan
2. `docs/TESTING.md`
3. This file

`docs/HANDOFF-2026-08-16.md` remains the last full opening-gate evidence pack. Do not treat it as the current content inventory.

## What actually exists (audited 2026-08-17)

Proven earlier (08-16, not re-run tonight): Boot → Prologue → Entry Hall → Parlor → Court → Shut the Box → Hidden Room → Labyrinth → ending is a PlayMode scene spine. That proves reachability. It does not make the five missing table games, or a fully walkable house.

**Hub Session 1 is on disk and was rebuilt.** Foyer crossroads, walkable stairs, 2F Percival bedroom, physically enforced doors (`GmEstateDoor` + solid `DoorBarrier`). Entry Hall GUI tour 12/12 tonight, **FLAKY**: attempt 1 idle-stalled, attempt 2 passed. Shots: `docs/playtest/screenshots/entry-hall-tour-*.png`.

**Hub Session 2 (North Library) is authored C# only.** `GmEntryHallBuilder.BuildNorthLibrary`, `GmWeightedShelf`, EditMode tests for aisle walk and lever→cellar. At audit time this had **not** been synced into `unity-project/` and had **not** been rebuilt or toured. The 12 tour frames still show the library *door*, not a furnished library.

**Parlor physical slice, House Memory, accessibility, Aldric production hook** are in the same dirty tree. They were not re-verified in this snapshot session. Treat them as source on disk, not shipping-proven tonight.

## Conflicts found

1. Several chats were rooted in `/Users/damato/Games Master` and tried to `move_agent_to_root`. One abort left Session 2 half-written in authored source while Unity was still touring Session 1.
2. Trackers disagree. STEAM-TRACKER still said Entry Hall/Parlor 0%. TASKBOARD said Session 2 not started. Authored C# already had the library. The 08-16 handoff said 455/455 EditMode; later sessions claimed 886/886 and 900/900. Inherit none of those numbers. Re-measure.
3. `unity-project/Assets/InitTestScene*.unity`, `TestResults/`, and `mono_crash.*` are Unity leftovers. Ignored, not committed.

## Next work (do not skip ahead)

1. Sync authored `unity/` into `unity-project/` if drift remains, rebuild `entry-hall`, audit, EditMode for library tests, new tour shots of the actual room.
2. Hub Sessions 3–6: 2F gallery/Marr, attic, cellar/vault, remaining game doors.
3. Then Parlor shipping proof, then the five missing table games. Court stays behind Nick's Phase 0 walk.

## Human gates (still Nick)

Physical-controller feel, display brightness, audio/wind character, figure subtlety, pacing, fear, arrival-vehicle taste, Aldric character art. Phase 0 walk is the opening exit gate.
