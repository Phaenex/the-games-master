# Handoff — 2026-08-16

64 local commits, nothing pushed. The working tree is intentionally **dirty** with the current
implementation and generated Unity outputs. Do not describe it as clean and do not commit without
Nick's explicit authorization.

Read `docs/TESTING.md` before running Unity. Do not chain Unity jobs, trust only the log written by
the current run, and do not waive the host-load gate.

## Current verified state

| | State |
|---|---|
| EditMode | **455/455** |
| PlayMode | **18/18** |
| Opening gates | **14/14 passed, 0 failed, 0 skipped** |
| Harness meta-gate | **22/22 paired reject/accept cases** |
| Opening contract | **20/20** |
| Standalone pacing | **p95 10.25 ms**, halves 10.23/10.30 ms, load 0.48/core |
| Route | **435/435 m covered**, 393.29 m controller travel, 0 stalls, 0 straight-line fallbacks, p95 11.36 ms |
| Wall proof | **6/6**, including both estate-gate attacks at ±55 m |
| Pixel evidence | **110/110 exterior, 6/6 physical, 10/10 interior clean** |
| Standalone composition | **8/8**, including distinct chapel and cemetery frames; 11/11 player captures clean |

The production chain is now authored and PlayMode-proven through:

**Boot → Prologue → Entry Hall → Parlor → Court → Shut the Box → Hidden Room → Labyrinth → ending.**

That proves scene completion and transition wiring. It does **not** turn the absent game content
into finished games.

## Reconciliation of the old 35-item tracker

The stale #28 is complete, as Nick noted. The other nine rows from the handoff list are now resolved
at the scope those rows claimed:

- **#12:** both walk stalls are gone; the fresh built-player route covered 435/435 m with zero stalls.
- **#19:** gate wings now follow the terrain to the boundary; ±55 m attacks held physically.
- **#22:** the measured tail was presentation wait/quantization, not GC or gameplay. Fresh standalone
  and full-route p95 are 10.25 ms and 11.36 ms on a trusted host.
- **#23:** the shipping opening now owns a `GmWendCompositionPlan` and passes the same strict local-
  light and review-claim audit as the generated rooms.
- **#28:** complete in `f0e6560`, 6 tells / 8 innocent.
- **#37:** the rebuilt porch frame was inspected directly; no trunk covers the front door.
- **#38:** every authored scene now has a production completion/exit path, and the full chain passes
  PlayMode. Five promised table games are still absent content and remain a separate product gap.
- **#39:** Entry Hall, Parlor and the other interiors have authored cool window separation; 10/10
  current interior evidence frames pass the wider, explicitly indoor cast band.
- **#40:** cold-open evidence is classified exactly, wall frames no longer misuse a sky heuristic,
  and every current exterior/physical/interior frame passes its applicable scanner.
- **#41:** Entry Hall and Parlor use owned Victorian meshes rather than all-primitive blockout.

## New defect found while closing #40

The rebuilt arrival shot exposed a defect stack that all prior tests missed:

1. purchased cliff visuals overhung the route after their colliders had been carved;
2. the arrival camera framed black terrain instead of a readable vehicle;
3. every car material slot had been flattened to one near-black material;
4. all four wheels were above the body because the FBX axis correction was overwritten;
5. the grille faced away from the route and the new lamps were mounted on the rear.

The builder now carves visual cliff siblings, preserves semantic body/glass/grille/wheel materials,
adds two visible warm period-lamp fixtures and a cool environmental moon key, rolls the imported car
upright, and points its grille up the estate route. Each structural fix has a regression test and the
saved-scene contract checks the final wheel/body/grille relationships. The current arrival frame was
inspected directly and passes the exterior scanner.

## Remaining work, in dependency order

1. **Nick's Phase 0 walk.** Automation proves bindings, collisions, sequence, pacing numbers and
   captures. It cannot judge controller feel, display brightness, wind/audio character, fear, or
   whether the manor figure is too obvious.
2. **Nick's arrival-vehicle taste gate.** The current donor is contemporary on purpose: the guest
   arrives from the present day, as recorded in `docs/playtest/unity-opening-asset-palette.md`. It is
   the curated car currently implemented by the opening, not the repository's only car mesh. Keep it
   unless Nick rejects the look; do not turn a pending human art call into an automatic replacement.
3. **Build the five missing table games.** Three have names in current planning material; two still
   have no canonical identities, and none of the named three has a complete ruleset. The signatures
   are only: Bones = inspectable loaded dice/push-your-luck; Study = chess with one visible Arbiter
   override; Wager = a deal whose hidden clause can be read with earned gold. They do not define turn
   order, legal actions, win/loss, match length, penalties, or game order. Do not manufacture those
   rules. Nick needs to name games 6–7 and approve complete rules/order for games 3–7; then build each
   as rules/runtime/scene/completion/evidence slices.

## Cemetery proof final slice

The built player now captures eight composition frames. `05-chapel-composition.png` remains the
chapel view, while `06-cemetery-composition.png` looks from the weathered-marker axis through the
fallen and child markers. The first two camera offsets both passed automation and failed direct
review because a tree trunk owned the frame. The durable fix protects the player-height marker
reveal during scene generation, uses a deterministic scatter, and adds a measured 2.6 m owned cross
monument. The saved-scene contract rejects vegetation intrusions in the reveal.

After rebuilding, the opening contract passes 20/20, EditMode passes 455/455, the standalone proof
passes 8/8 at 10.25 ms p95, and all 11 standalone captures pass the night-frame scanner. The full
14/14 gate stack then requalified the current source with 0 failed and 0 skipped. The final cemetery
frame was inspected directly after that rebuild; it reads as a wooded burial ground with named
markers, crosses, the monument and chapel lights, but final AAA art approval remains Nick's taste gate.

The remaining Phase 0 instrumentation gap is closed too: `GmWendPerformance` no longer excludes
`HouseBeginning`. A fresh rebuild audits all 233 interior renderers, preserves them for runtime
culling, and the saved-scene contract rejects any interior renderer disabled before the culler can
discover it. The regression was observed red; EditMode is now 455/455 and the opening contract is
20/20.

## Hard rules

- No push. No commit without Nick's explicit authorization.
- No asset or tool purchases without asking.
- Never hand-edit `.unity` or `.prefab` YAML. Author in `unity/`, sync, then rebuild.
- The house's own front doors never open on foot. The Ninth Bell transition remains canonical.
- A green test is a floor. Inspect the saved scene and the current image before making a visual claim.
