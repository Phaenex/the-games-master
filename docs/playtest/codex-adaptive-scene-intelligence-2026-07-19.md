# Adaptive scene intelligence implementation report - 2026-07-19

## Verdict

The supervised scene-intelligence engine is implemented and production-proven on Wend Hill without
changing the accepted visual transform layout. Engine grade: **A**. It is not A+ yet because it has
not prevented and corrected mistakes during a brand-new production room, and Nick has not approved
a visual baseline. Current Wend Hill art grade remains **A- pending Nick's walk**, not upgraded by
metadata.

## Delivered

- Deterministic, atomic, schema-checked scene memory with immutable review sessions.
- Nick/agent weighting, automation taste lockout, contextual exclusions, draft rules, explicit
  rule promotion, and append-only defect resolution.
- Imported-asset profiling and rendered contact sheets for 1,676 existing Unity assets.
- Scene semantics for zones, story clusters, roles, routes, negative space, lights, audio, and shot
  claims.
- Two guarded Wend Hill adaptive slots with three candidates each and no selected override.
- Perceptual and objective visual-baseline measurements with approval blockers.
- A Unity review console for evidence, verdicts, walking, captures, variants, baselines, promotion,
  and resolution.
- A reusable one-command macOS player-backbuffer proof.

## Production proof

| Gate | Result |
|---|---|
| Knowledge tests | 11/11 PASS |
| Scene-system tests | 10/10 PASS |
| Fresh scaffold compile | PASS against Unity 6000.5.3f1 |
| Repo-to-Unity sync | 22/22 tracked C# files clean |
| Saved-scene intelligence | PASS: 8 zones, 15 clusters, 57 elements, 2 slots |
| Estate/composition audit | PASS, fingerprint `cb198edc4905ac2e` |
| Asset index | 1,676 assets; repeated bytes identical |
| Guarded variants | 6/6 real HDRP PNGs; selected none; exact restoration |
| Unity EditMode | 61/61 PASS |
| Unity PlayMode | 3/3 PASS |
| HDRP editor tour | 18/18 PASS |
| macOS build | PASS |
| Standalone player proof | 7/7 backbuffer PNGs; zero runtime errors; no UnityEditor assembly |
| Shut the Box rules | 23/23 PASS |
| Browser regression harness | 47/47 PASS |

The injected-exception test proves variant restoration in a failure path. The serialized-scene test
closes and reopens Wend Hill before auditing its intelligence metadata. `adaptive-overrides.json`,
`approved-rules.json`, `defect-resolutions.json`, and `baselines.json` remain empty after proof.
The Unity-to-Node serialization test also caught and closes Unity's optional-string behavior:
`JsonUtility` emits unused strings as empty, and ingestion strips only those known optional fields
before schema validation and canonical storage.

## Visual inspection

The editor tour and fresh standalone frames are real, nonblank HDRP output. Spawn and porch depth
remain strong. Cemetery and garden are readable, but they visibly preserve the known limits: the
graveyard still exposes repeated kit language, and the garden is functional rather than a hero
composition. The engine correctly reports those defects instead of pretending its structural pass
is an art pass.

## Grade and next proof

**A for infrastructure, A- for the current opening candidate, not A+.** To earn A+ for the engine,
use it from the first blockout of Entry Hall, let its audits catch real authoring mistakes, compare
guarded alternatives, obtain Nick's Keep/Change/Reject evidence, promote only a genuinely recurring
rule, and approve the first baseline only after the room itself is accepted.

No asset purchase, commit, push, second mansion, adaptive selection, visual-baseline approval, or
Threshold Refusal opening occurred.
