# Codex Handoff Guide — The Games Master

> **Read this first.** Then execute `docs/superpowers/plans/2026-07-15-codex-takeover.md`.
> Owner: Nick (human). Prior agent context ends 2026-07-15. You are taking over.

> **2026-07-18 supersession:** The shipping target is now a Steam game in Unity at
> `~/GamesMaster-Unity`. The browser build is design and regression reference only. Current delivery
> truth lives in `docs/STEAM-TRACKER.md`; `docs/PROGRESS.md` is the chronological work log. Phase 0
> ends on the ninth bell wherever the player stands, not with the retired porch-only KO. Use
> `docs/UNITY-SCENE-WORKFLOW.md` for every new room. Where an older section below conflicts with this
> paragraph, this paragraph and the dated Steam tracker win.

---

## 60-second orientation

| Item | Truth |
|------|--------|
| What it is | First-person psychological-horror Steam game in Unity HDRP |
| Unity project | `~/GamesMaster-Unity`, pinned to 6000.5.3f1 |
| Shipping opening | `Assets/Scenes/WendHill.unity`, generated by `GmEstateBuilderV2` |
| Browser files | Retired product; useful as design source and regression archaeology |
| Tracker | `docs/STEAM-TRACKER.md` (Unity delivery truth; Nick Phase 0 walk still open) |
| Catalog | `asset-catalog.html` (stages 0–8 + `#buy-next`) |
| Tests | `npm test` plus Unity EditMode/PlayMode through `scripts/unity-cli.mjs` |
| Visual proofs | `~/GamesMaster-Unity/Screens/<scene>/` from registered review tours |
| Human gate | Nick must walk the opening before Court is “done” |

Serve locally:

```bash
cd ~/Projects/the-games-master
npx --yes serve -l 3456 .
# http://localhost:3456/The%20Games%20Master%20-%20Prologue.dc.html
```

Hard refresh after every code change (Cmd+Shift+R).

---

## Hard rules (do not violate)

1. **Do not push** to `origin` without Nick’s explicit approval.
2. **Do not commit** unless Nick asks (or he says Codex may commit).
3. **No second mansion purchase.** Threshold Refusal is locked — front doors stay closed through KO. Spec: `docs/superpowers/specs/2026-07-14-opening-threshold.md`.
4. **Buy list** is trees/ground/SFX only (~$20–40). Locked out: Modular English, KitBash, Synty mansion, another Env shell, door megapacks. Catalog `#buy-next`.
5. House history = **coaching inn**, not saloon. Spec: `docs/superpowers/specs/2026-07-14-house-history.md`.
6. **Visual gate before claiming UI done:** run app, click what you touched, save screenshots, read them. Unit tests alone ≠ verified. Log PASS / BORDERLINE / FAIL honestly.
7. Do **not** load full Env Modular GLBs (31–45MB). Use `assets/models/unity/victorian-mansion-environment/web/*_LOD0.glb` from `scripts/strip-env-lod0.mjs`.
8. Do **not** remount `SM_Wall_Door` as door surround (solid plate buries the opening).
9. Do **not** reopen the door fashion show (swing open → vestibule → KO). Closed leaves through blackout.
10. Update `docs/PROGRESS.md` at session end when status changes.
11. **Global progress rule:** every Codex check-in must include the current overall progress bar, phase bars, completed work since the last check-in, and the next blocker. `docs/STEAM-TRACKER.md` is the percentage source of truth; `docs/PROGRESS.md` records dated evidence. Refresh both before reporting changed status. Never present a stale percentage as current.

Nick-only vs agent: `docs/NICK-NEEDED.md`.

---

## Canon beat you must preserve (opening)

**Threshold Refusal:** walk drive → gate locks → porch → doors stay shut → something from porch dark → KO thud → wake Entry Hall (“No memory of a doorway.”).

Side/servant path = optional later polish only. Not required.

---

## Takeover sequence (run in order)

### Phase A — Cold review (no code yet)

Produce a short written audit in chat (or `docs/playtest/codex-review-YYYY-MM-DD.md`):

1. Read: this file · `docs/PROGRESS.md` · `docs/NICK-NEEDED.md` · opening-threshold spec · house-history spec · last 3 CHECKIN entries in PROGRESS.
2. Run: `npm test`.
3. Run harnesses (expect errors:0):
   - `node scripts/play-door.mjs`
   - `node scripts/play-full.mjs` (or `play-gate.mjs` if full is slow)
4. Open screenshots and **read them** (don’t trust captions alone):
   - `docs/playtest/screenshots/door-01`…`door-06`, `door-03b-porch-dark.png`
   - `begin-outgate-prebuy.png` / `begin-outgate-trees.png`
   - any `env-*`, `gate-*` recent
5. Classify every issue: **BLOCKING** / **POLISH** / **BUY** / **NICK** / **WONTFIX** (canon).

Ship the audit table before editing.

### Phase B — Fix what’s agent-owned (opening)

Work from the takeover plan Task 1–N. Priority order:

1. Regressions / pageerrors from harnesses
2. Floating light orbs, pale bleach, green foliage maps
3. Door readability at porch distance (Threshold Refusal, doors shut)
4. Outer look-back tree read (owned BotD + Polytope already wired; don’t buy yet)
5. Harness/docs accuracy after each batch

Re-shot after each visual batch. `npm test` green before claiming done.

**Stop and ask Nick** if the next step is “buy an asset pack” or “play Court as complete.”

### Phase C — Await Nick Phase 0 walk

Do not treat Court polish as Phase 0 complete. Nick walk is the gate.

After he reports, fold notes into PROGRESS + fix only what he flagged. Then his **buy call** (Dead Tree Pack yes/no).

### Phase D — Continue roadmap

Only after Phase 0 human gate (or Nick explicitly says proceed):

Follow `docs/superpowers/plans/2026-07-15-codex-takeover.md` Part 2 (phases 1→9) and earlier plans:

| Phase | Plan / notes |
|-------|----------------|
| 1 Court | `docs/superpowers/plans/2026-07-14-court-and-shut-the-box.md` — shell exists; finish match |
| 2 STB | same plan + `gm-shutbox-logic.js` (23 tests already) |
| 3 Shards | roadmap |
| 4 Hidden room | after STB tile-9 |
| 5 Persistence | after multi-room consumers |
| 6 Labyrinth | last among rooms |
| 7 Endings | after panels |
| 8 Manor puzzles | deferrable |
| 9 Final polish | Nick + AI panel |

Playtest cadence: Nick after 0, 2, 7, 9. AI panel after 2, 7, 9 **only**.

---

## Key file map

| Want | Where |
|------|--------|
| Unity scene workflow | `docs/UNITY-SCENE-WORKFLOW.md` |
| Scene command registry | `unity/scene-system/scene-registry.json` |
| Scene scaffold and sync | `scripts/scaffold-unity-scene.mjs`, `scripts/sync-unity-scenes.mjs` |
| Unity command driver | `scripts/unity-cli.mjs` |
| Shared scene contract source | `unity/scene-system/Runtime/`, `unity/scene-system/Editor/` |
| Wend Hill build/audit | `~/GamesMaster-Unity/Assets/Editor/GmEstateBuilderV2.cs`, `GmEstateQualityAudit.cs` |
| Unity review images | `~/GamesMaster-Unity/Screens/<scene>/` |
| Opening scene (huge) | `The Games Master - Prologue.dc.html` |
| Owned dead trees | `_mountOwnedDeadTrees` in Prologue |
| Porch lamps / doors | `_mountEnvPorchLamps`, `_mountOwnedFrontDoors`, `_dressDoorPanelOverlays` |
| Ruins grounds | `buildRuinsGrounds` |
| Env fence LOD0 | `_dressEnvOuterWalls`, `scripts/strip-env-lod0.mjs` |
| SFX credits | `assets/sfx/license.txt` |
| Court / STB shells | `The Games Master - Court.dc.html`, `…Shut the Box.dc.html` |
| STB pure logic | `gm-shutbox-logic.js`, `scripts/test-shutbox.mjs` |
| Shared doubt/settings | `gm-doubt.js`, `gm-settings.js`, `gm-progress.js` |
| Court props | `gm-court-props.js` |
| Live walk harnesses | `scripts/play-*.mjs`, `verify-*.mjs`, `capture-*.mjs` |

Prologue is one large file. Prefer surgical edits; don’t “refactor the prologue” unless Nick asks.

---

## Known residual (as of 2026-07-15 — verify, don’t assume stale)

Agent-owned (likely still true):

- Gravyart porch/facade still reads flat/grey vs AAA horror
- `ruins_pack` trees still low-poly under owned BotD/Polytope accents
- Outer grounds look gap → **buy Dead Tree Pack only after Nick walk**
- Court/STB = shells, not full matches
- Env full facade assemble still deferred (no hinged doors in Env)

Fixed / closed (re-verify if you touch them):

- Threshold Refusal timing + porch side-dark + `ko_thud.ogg`
- Gate/KO SFX: project-authored Opus; license.txt updated
- Porch + drive MeshBasic glow spheres removed (cages instead)
- Door_Double readability via canvas panel overlays (shells hidden)
- Bush atlas spring-green killed (night MeshBasic)
- Car HD 03 wired; Model T retired
- Env fence LOD0 on outer look-back; SM_Wall_Door rejected

---

## Verification commands (cheatsheet)

```bash
npm run unity:scene:registry
npm run unity:scene:check
npm run test:scene-system
npm run test:scene-system:csharp
node scripts/unity-cli.mjs host-health
node scripts/unity-cli.mjs rebuild wend-hill
node scripts/unity-cli.mjs audit wend-hill
node scripts/unity-cli.mjs test
node scripts/unity-cli.mjs playtest
node scripts/unity-cli.mjs tour wend-hill
npm run unity:build:mac

# Retired browser regression archaeology follows.
npm test
node scripts/play-door.mjs          # porch → KO → aftermath
node scripts/play-full.mjs          # longer walk (if present)
node scripts/play-gate.mjs          # gate lock
node scripts/verify-env-entrance.mjs
node scripts/verify-handoff.mjs     # aftermath → Entry Hall
```

Screenshot dest: `docs/playtest/screenshots/`. Name with intent (`codex-outgate-01.png`).

---

## How to talk to Nick

- Lead with verdict + evidence (commands + screenshot paths).
- Never say “verified” from unit tests alone.
- Never ask him to eyeball prod before you ran the visual gate.
- Ask before buy, push, commit, or reopening Threshold Refusal.
- Session end: update PROGRESS bar + CHECKIN if you changed status.

---

## Start prompt (paste into Codex)

```
You are taking over The Games Master at ~/Projects/the-games-master.
1) Read docs/CODEX-HANDOFF.md completely.
2) Run Phase A cold review (no code until audit table is written).
3) Then execute docs/superpowers/plans/2026-07-15-codex-takeover.md Part 1.
Hard rules: no push, no second mansion, Threshold Refusal stays closed-door,
visual gate required, ask Nick before buys/commits.
```
